using UnityEngine;

public class condicion3 : MonoBehaviour
{

    private void Awake()
    {
        condicion1.miob.name = "nuevo ob";
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
