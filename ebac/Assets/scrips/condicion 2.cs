using UnityEngine;

public class condicion2 : MonoBehaviour
{

    private void Awake()
    {

    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log(condicion1.miob.name);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
